export type NewsItem = {
    id: string
    title: string
    description: string
    url: string
    image?: string
    source: string
    category: string
    language: string
    publishedAt: number
}